# R script to remove puct chars from axciton file names

library(stringr)

# shortNames <- str_replace_all(gsub("\\$", "", OSS2_caseList_2018$AxcitonFileName), "[:punct:]", "")

# criterionState$examName <- 
#   str_replace_all(gsub("\\$", "", criterionState$ID), "[:punct:]", "")
  
  